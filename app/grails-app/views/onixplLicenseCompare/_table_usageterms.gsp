<thead>
  <tr><g:each var="heading" in="${headings}">
    <th>${heading}</th>
  </g:each></tr>
</thead>
<tbody><g:each var="row_key, row" in="data">
  <tr>
    <td>${  }</td>
  </tr>
</g:each></tbody>